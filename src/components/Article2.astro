---
import { Image } from 'astro:assets';
import myImage from '../assets/thumbNail2.png';
import type { EntryFieldTypes } from 'contentful';
import type { DocumentNode } from '../types/contentful';
interface Props {
	title: EntryFieldTypes.Text;
	body: DocumentNode;
  imageURL: string;
}
const {title, body, imageURL } = Astro.props;
---

<article>
  <div class="thumbnail">
    <Image src={myImage} alt="Thumbnail" width="160" height="160" />
  </div>
  <div class="content">
    <div class="category">
      <p>Tag/Category</p>
    </div>
    <div class="heading">
      <p>{title}</p>
    </div>
    <div class="description">    
      <p>{body.content[0].content[0].value}
         </p>
      </div>
  </div>
</article>

<style>
  article {
    display: flex;
    height: 200px;
  }
  .thumbnail {
    margin-right: 20px;
  }
  .thumbnail img {
    max-width: 100%;
    height: auto;
  }
  .content {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    margin-bottom: 40px;
  }
  .category {
    height: 30px;
    font-size: 14px;
    color: #3CAD71;
  }
  .heading {
    display: flex;
    flex-direction: column;
    justify-content: center;
    margin: 0px;
    font-size: 20px;
    font-weight: bold;
    max-width: 280px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .description {
    margin: 0px;
    max-width: 280px;
    font-size: 16px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  h6{
    margin: 0px;
  }
  p{
    margin: 0px;
  }
</style>
